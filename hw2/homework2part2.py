# The goal: Extract the first four columns from serial_log.txt and place them in output1.csv
#
# Hint: to join a list of strings into one string, use the .join() function:
#   ','.join(['256', '24.525', '103.70'])
# yields
#   '256,24.525,103.70'
